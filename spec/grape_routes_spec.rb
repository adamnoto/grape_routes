require 'spec_helper'

describe GrapeRoutes do
  it 'has a version number' do
    expect(GrapeRoutes::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
